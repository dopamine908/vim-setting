.match : php match
    ANY  →  match ($expr$) { \
				$FIRST$ => $SECOND$, \
				default: $END$ \
			};

.fake : factory fake
	ANY   →   \$test$expr$ = $expr$::factory()->forTest()->create();


.str : string format
	ANY   →   '$expr$'
